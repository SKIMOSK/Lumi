<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"/>
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true"/>

    <!-- Audio / Microphone -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>

    <!-- Camera -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <!-- Contacts -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <!-- Notifications & messages -->
    <uses-permission android:name="android.permission.RECEIVE_SMS"/>
    <uses-permission android:name="android.permission.READ_SMS"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <!-- Alarms / exact timers -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>

    <!-- System settings control -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS"/>
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Calendar -->
    <uses-permission android:name="android.permission.READ_CALENDAR"/>
    <uses-permission android:name="android.permission.WRITE_CALENDAR"/>

    <!-- Internet (Gemini API) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Location — GPS for navigation cues + BLE on older APIs -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>

    <!-- Health Connect -->
    <uses-permission android:name="android.permission.health.READ_STEPS"/>
    <uses-permission android:name="android.permission.health.READ_SLEEP"/>
    <uses-permission android:name="android.permission.health.READ_HEART_RATE"/>
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED"/>
    <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED"/>

    <!-- Write contacts -->
    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>

    <!-- Wake lock for background processing -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE"/>

    <!-- Vibrate for feedback -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Gallery / media -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <!-- Package visibility (Android 11+) -->
    <queries>
        <!-- Messaging -->
        <package android:name="com.whatsapp"/>
        <package android:name="com.whatsapp.w4b"/>
        <package android:name="com.instagram.android"/>
        <package android:name="com.snapchat.android"/>
        <package android:name="com.facebook.orca"/>
        <package android:name="com.discord"/>
        <package android:name="org.telegram.messenger"/>
        <package android:name="com.Slack"/>
        <!-- Notes apps -->
        <package android:name="com.samsung.android.app.notes"/>
        <package android:name="com.google.android.keep"/>
        <package android:name="com.microsoft.office.onenote"/>
        <package android:name="notion.id"/>
        <package android:name="md.obsidian"/>
        <package android:name="com.standardnotes.standardnotes"/>
        <!-- Email -->
        <package android:name="com.google.android.gm"/>
        <!-- Navigation -->
        <package android:name="com.google.android.apps.maps"/>
        <package android:name="com.waze"/>
        <!-- VPN -->
        <package android:name="com.surfshark.vpnclient.android"/>
        <package android:name="com.nordvpn.android"/>
        <!-- Streaming -->
        <package android:name="com.spotify.music"/>
        <package android:name="com.google.android.apps.youtube.music"/>
        <package android:name="com.netflix.mediaclient"/>
        <!-- Smart Home -->
        <package android:name="com.google.android.apps.chromecast.app"/>
        <!-- Health -->
        <package android:name="com.google.android.apps.healthdata"/>
        <package android:name="com.google.android.apps.fitness"/>
        <package android:name="com.strava"/>
        <package android:name="com.myfitnesspal.android"/>
        <!-- Banking -->
        <package android:name="com.revolut.revolut"/>
        <package android:name="ro.btrl.mobile"/>
        <package android:name="com.transferwise.android"/>
        <package android:name="com.paypal.android.p2pmobile"/>
        <!-- Shopping -->
        <package android:name="com.amazon.mShoppingApp"/>
        <!-- Crypto -->
        <package android:name="com.binance.dev"/>
        <package android:name="com.coinbase.android"/>
        <package android:name="io.metamask"/>
        <package android:name="com.wallet.crypto.trustapp"/>
        <!-- News -->
        <package android:name="com.google.android.apps.magazines"/>
        <!-- YouTube -->
        <package android:name="com.google.android.youtube"/>
        <!-- Ride sharing -->
        <package android:name="com.ubercab"/>
        <package android:name="me.lyft.android"/>
        <!-- Food delivery -->
        <package android:name="com.ubercab.eats"/>
        <package android:name="com.dd.consumer"/>
        <package android:name="com.deliveroo.orderapp"/>
        <intent>
            <action android:name="android.intent.action.CALL"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.INSERT"/>
            <data android:mimeType="vnd.android.cursor.dir/event"/>
        </intent>
        <intent>
            <action android:name="android.provider.AlarmClock.SET_TIMER"/>
        </intent>
    </queries>

    <application
        android:name=".LumiApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lumi"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".settings.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"/>

        <activity
            android:name=".ui.PatchNotesActivity"
            android:exported="false"
            android:parentActivityName=".settings.SettingsActivity"/>

        <!-- Notification Listener Service -->
        <service
            android:name=".notifications.LumiNotificationService"
            android:exported="true"
            android:label="@string/notification_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService"/>
            </intent-filter>
            <meta-data
                android:name="android.service.notification.default_filter_types"
                android:value="conversations|alerting"/>
        </service>

        <!-- Bluetooth foreground service -->
        <service
            android:name=".bluetooth.LumiBluetoothService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice"/>

        <!-- Geofence transitions receiver -->
        <receiver
            android:name=".location.GeofenceBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.lumi.app.ACTION_GEOFENCE_TRANSITION"/>
            </intent-filter>
        </receiver>

        <!-- Timer alarm receiver -->
        <receiver
            android:name=".timer.TimerReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.lumi.app.ACTION_TIMER_DONE"/>
            </intent-filter>
        </receiver>

        <!-- FileProvider for sharing cached images (base64 → file → intent) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths"/>
        </provider>

        <!-- Accessibility service for WhatsApp reading -->
        <service
            android:name=".whatsapp.LumiAccessibilityService"
            android:exported="true"
            android:label="Lumi Accesibilitate"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config"/>
        </service>

    </application>
</manifest>
